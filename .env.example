## Email via Resend SDK
# Copy this file to .env and fill real values

# Resend API key (starts with re_...)
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxx

# The From header. Use a verified sender/domain in Resend.
FROM_EMAIL=Afterschool <no-reply@yourdomain.com>

# Where admin notifications should go
ADMIN_EMAIL=payments@exceedlearningcenterny.com

# Optional server port
PORT=3001

## Optional: Use SMTP (e.g., Gmail) instead of Resend
# Set provider to 'smtp' to use the SMTP settings below. Leave empty or 'resend' to use Resend API.
EMAIL_PROVIDER=smtp

# Gmail SMTP settings (recommended with App Passwords)
# For Gmail: SMTP_HOST=smtp.gmail.com, SMTP_PORT=465 with SMTP_SECURE=true (or 587 with false)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=yourgmail@gmail.com
SMTP_PASS=your_app_password

# Frontend API base (used by src/App.tsx).
# Dev: you can leave this empty; the app will call http://localhost:3001 automatically.
# Prod: set to your deployed server URL (no trailing slash), e.g. https://olga-one-page-form.onrender.com
VITE_API_BASE=

## Stripe (Checkout + Link)
# Secret key for your Stripe account (starts with sk_test_ or sk_live_)
STRIPE_SECRET_KEY=sk_test_xxxxxxxxxxxxxxxxxxxxxxxxx

# Webhook signing secret (starts with whsec_). Required if you use the webhook route.
# Get one by running:  stripe listen --forward-to localhost:3001/api/payments/stripe/webhook
STRIPE_WEBHOOK_SECRET=whsec_xxxxxxxxxxxxxxxxxxxxxxxx

# Frontend URL used for Stripe success/cancel redirects
# Dev default is http://localhost:5173; set explicitly if different.
FRONTEND_URL=http://localhost:5173

# Notes:
# 1) Enable Link in Stripe Dashboard → Settings → Payment methods → Link.
# 2) Start local webhook listener when testing:
#    stripe listen --forward-to localhost:3001/api/payments/stripe/webhook
# 3) Restart the API after changing env: npm run server
